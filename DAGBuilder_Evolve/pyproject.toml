[project]
name = "dagbuilder-evolve"
version = "0.1.0"
description = "Simulation-driven parallel strategy evolution for DAGBuilder"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

